package com.amtkxa.usecase.core.usecase

/**
 * This interface represents a request for a use case. One should create specific interface
 * for given use case by extending this one.
 */
interface Request
